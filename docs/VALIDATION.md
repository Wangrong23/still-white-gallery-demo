# MVP 验证记录

环境：Windows；Node 22+；Three.js 0.180.0；ws 8.21.3；桌面 Chromium 内嵌浏览器。

## 自动测试

- `node scripts/check.js`：所有 JS 模块语法检查。
- `node --test tests/core.test.js tests/network.test.js`：游戏规则及真实 WebSocket 集成用例通过，具体数量以当前测试输出为准。
- `node scripts/build.js`：生成离线依赖齐全的浏览器资源目录。
- 依赖审计：升级 ws 后复核，无已知漏洞；隔离目录 `npm ci` 安装恰好两个依赖成功。

## 实际运行

- HTTP `/health` 返回 200；首页和 Three 模块正常加载；浏览器错误日志为空。
- 以凶手进入单人演练；F10 进入中央展台。HUD 显示 STILL、SPOT 0；人物一直存在。
- 高太阳时刻，人物与展台产生短影。连续 F3 后，太阳降至约 23° 并横移；同一人物的影子明显变长、转向，墙和其他雕塑的阴影也同步变化。
- Tab 切到警探、F4 进入 SUNSET，之后 NIGHT / RUN；画面变暗，锥形手电照亮前方且周边仍黑暗。
- 夜间计时结束显示 SURVIVED / DETECTIVE WINS，能返回菜单。
- 浏览器双窗口：点击 HOST 生成房间码；第二窗口 JOIN 同一码，自动获得另一角色；两窗口分别显示 DETECTIVE / KILLER 并进入同一局 DAY。双 WebSocket 权威状态和双方重开另有真实连接集成测试。

## 实测边界

尚未进行两个真人在互联网延迟下的完整 7 分钟对局，也未完成长时间服务器负载测试。浏览器画面检查验证了真实阴影机制，不等于藏点与胜率已经平衡。公网部署需要支持长驻 Node + WebSocket 的运行环境。


## 2026-09-05 试玩反馈修复

- 新增近身检查、无弹抓捕、检查中断与遮挡测试；假人碎裂、碰撞移除、贴面弹孔、警戒触发 / 过期、快照插值测试。
- WebSocket 集成覆盖双方同步假人碎裂、警戒、无弹抓捕；重开清空残骸与警戒状态。
- 在 Codex 内嵌浏览器 1280×720 验证了真实 Gallery 渲染：假人消失并留下落地碎块；假人和空射不产生弹孔；墙面弹孔为贴面圆片；重开恢复假人并清空弹孔；警戒与触发提示可见。8 项浏览器断言通过，控制台无错误或警告。
- 同一验证视角测得 80 个预热后帧间隔中位数约 5.0ms、P95 约 5.1ms，绘制调用 185。这只是当前机器的单一测试视角，不能代表所有设备或真人联网体验。
- 渲染优化：静态轮廓线合批、太阳阴影从 4096 降为 2048、HUD 更新限为 15Hz、缓存陪练寻路可通行网格。警探相机取消步行上下抖动；第三人称镜头避障后平滑拉远。
- 联机采用 100ms 快照缓冲插值，保持服务器状态不变；尚未实现本地移动预测，真实高延迟双人试玩仍需验证。


## 2026-09-06 屏息交互调整

- 15 秒气量、1 秒回气延迟、5 秒耗尽冷却；重新开始至少需要 3 秒气量。持续按住直到耗尽仅产生一次喘气，过早按键须松开再试。
- 回归覆盖连续耗尽、冷却恢复、反复点按、动画起停连续性、权威命中与插值位移一致性、屏息延缓抓捕及切换屏息无法重置检查进度、隔墙声音衰减、日落停止和重开复位。
- 普通呼吸约 1.2 秒识破；持续屏息或假人约 3.6 秒检查。WebSocket 集成验证双方均收到屏息和检查进度，松开后加速完成无弹抓捕，重开清空屏息锁定。
- 浏览器真实游戏 HUD 验证：气量上限 15 秒；低气量显示剩余秒数、橙色条和松开提示；耗尽显示 5 秒倒计时及必须松开空格的提示。验证时控制台无错误和警告。
- 本节记录屏息调整时的状态；自由静止随后实施，见下节。


## 2026-09-06 自由静止与特殊姿势

- E 在普通可站立地面原地冻结，F 使用最近可达特殊姿势；预览只向凶手显示。移动键重新按下可解除静止，快速点按也会立即发送解除动作。
- 特殊姿势用 0.3 秒权威位置与身体姿势插值，命中和渲染共用；路径检查拒绝隔墙入位、占用目标和非法地面。支持中途解除、返回点被占用时安全退出、日落解除。
- 新增自动测试覆盖自由静止、全部特殊姿势的合法进入位置、移动解除、按住移动时冻结、过渡命中、墙体阻隔、占用退出、警戒兼容、阶段限制及联机复制。
- 浏览器验证了普通地面 E 冻结、快速 W 解除、青绿色特殊姿势预览、F 进入雕塑姿态和相应 HUD 提示。


## 2026-09-06 黑白电影美术与原创资产

- Blender 5.0.1 后台生成 10 种单位尺寸身体网格模板，保留 `.blend` 源文件及脚本；凶手与假人共用石膏造型，警探使用风衣、礼帽和服装细节。仍为分部动画，未改为骨骼蒙皮。
- 新增原创装饰艺术展览海报、深色墙裙及顶边、天窗条带；建筑装饰实例合批。取消青绿色预览和橙色警告，改用灰度、反白和文字。
- 修正太阳阴影偏移及背面投影，减少墙面自阴影条纹。2048 阴影贴图仍可能在斜边和远处出现锯齿，未声称消除所有阴影瑕疵。
- 新增原创鞋跟、左轮、石膏碎裂和闭馆钟声 WAV。加载失败保留程序音效；闭馆语音按界面语言选中英文，具体音色依赖系统 TTS。
- `npm test`：40/40 通过；包括模板覆盖/命中范围、PCM 格式、HTTP MIME 类型及原有玩法和联机回归。`npm run build` 通过。
- 内嵌浏览器 1280×720 检查主菜单、日间雕塑视角、夜间警探手电和左轮；控制台未返回错误或警告。音效验证覆盖文件格式和交付链路，尚未进行耳机主观混音验收。
- 高延迟网络预测、真人平衡、精细装饰碰撞及完整角色骨骼仍保留在已知限制中，本轮没有将它们标为已解决。


## 2026-09-06 阴影藏身回归修正

- 试玩反馈后移除墙裙、踢脚线和顶边装饰，恢复连续纯色墙面。
- 查明默认 Toon 渐变将背光面照亮至 70% 档位，且日间 0.48 环境光绕过遮挡照亮人物。改为共享的零背光渐变及日间零环境补光；太阳阴影偏移缩小至 -0.0003，减少亮边泄漏。
- 用临时浏览器场景加载真实 Gallery，在 dayTime=60、凶手 (0,0,-12.45)、屏风南侧完整遮光位置，分别检查凶手和警探视角：身体融入黑色背景。移动至 (4,0,-10) 后受光头顶与肩部清晰显现。没有按区域隐藏角色，也没有对本地角色单独调暗。
- 夜间切换后手电仍照亮角色与墙面，夜间原有微弱环境光保留。检查控制台无错误/警告。临时场景验证后移除。
- 本次只验证了上述具体位置和视角；背后是亮面、身体部分受光或站在阴影边缘时，轮廓仍可能暴露，不能将“脚下有阴影”等同于完全隐蔽。


## 2026-09-06 深灰暗部调整

- 按试玩反馈撤销整幅画面的对比度滤镜；最终 CSS 不包含该滤镜。
- 在 Toon 材质的线性光照输出阶段设置共同暗部下限 0.028（显示约 #2f2f2f），只抬升低于下限的暗部，保留正常受光输出。人物、墙面、地面与画作共享该规则，避免恢复环境光造成不同材质的暗部差异。夜间与日落关闭此下限。
- 浏览器确认暗部呈深灰、亮部仍保持原亮度；未发现着色器错误。构建及语法检查通过。本次没有重复运行未受修改的玩法测试。


## 2026-09-06 基础设置、空间音效与装饰占位

- 新增中英文设置弹窗：总音量、环境底噪、灵敏度、纵轴反转、闭馆语音、渲染分辨率；本地保存、容错加载、恢复默认。性能模式降低渲染比例，未修改太阳、材质和阴影规则。
- WAV 脚步与合成回退共用墙体衰减/低通处理，左右声像按听者朝向计算；喘气保留原有 8m 规则。总音量包含 Web Audio 与 TTS，返回菜单取消播报、延迟音效和活动音源。
- 新增 7 个装饰落地占位，供权威移动、自由静止、姿势路径和陪练寻路共用；镜头避让实际可见装饰。弹道仍不使用这些占位盒，防止空中生成不可见阻挡和弹孔。精细枝叶/挂钩/雕塑弹道仍未完成。
- 新测试覆盖设置持久化、非法数据/存储失败、实时音量及音源清理、墙与门洞的声学差异、回头后的左右声像、全部装饰占位、穿越和静止拒绝、空中弹道兼容。
- 浏览器 1280×720 检查设置布局，40% 音量和纵轴反转在刷新后保留，恢复默认正常；暂停中打开设置再按 Esc 只关闭设置，对局保持暂停。返回菜单正常，检查期间控制台未报告错误/警告。主观声学混音仍需耳机试玩。
- `npm test`：47/47 通过；`npm run build` 通过。凶手静止时，监听朝向使用自由相机方向，避免身体锁定后左右声像也被锁住。


## 2026-09-06 断线重连

- 实现 30 秒角色保留、断线暂停、凭据恢复、当前标签页刷新恢复与主动退出清理。覆盖原局状态保留、错误凭据和普通加入不能占位、超时清理、客户端自动重试及保存会话恢复。
- 双浏览器页检查：凶手刷新后恢复为原角色及原对局进度，显示“双方连接已恢复”，等待点击继续；临时房间已退出。本地服务已更新。
- 按当前工作约定只做关键联机检查及常规构建；服务器重启恢复、客户端预测和高延迟补偿仍未完成。


## 沉浸文案收尾

中英文菜单、阶段提示、事件反馈和结局统一为克制的展馆/黑色电影语气，参数说明保留在随身手册。呼吸读数改为整秒，位置与姿势提示使用界面语言；广播改为闭馆通知。避免用“他还在看”等固定提示误报对手状态。构建与语法检查通过，未重复运行玩法回归。

## 关节动作与声音质感

- 肩肘与髋膝使用连接的分部关节链；步幅随实际位移推进，走停渐变，完成特殊姿势后增加 0.22 秒起身过渡。渲染和权威命中共用计算，凶手静止与假人共用姿势。仍非完整蒙皮、动作捕捉或世界空间脚底锁定；特殊藏点的退出位置仍沿用安全返回点。
- 重生成三种脚步、两种碎裂、左轮与钟声，加入共享短混响。中英文广播由本机 Microsoft Huihui / Zira 声音预生成，再经运行时扩音器处理；非真人录音。生成脚本与来源说明保留在 art/README.md。
- 增加关节端点连续性、静止与假人一致性、坐姿起身高度连续性的关键检查。常规回归与构建通过；本轮未重新完成浏览器视觉及耳机主观混音验收。墙面、阴影材质与藏身照明未调整。


## 贴墙、自动站定与白昼节奏

- F 动态选择普通墙面可达位置，复用姿势预览和权威过渡；墙角留边、路径阻挡与占位检查继续生效。外墙及内部屏风、移动退出均通过关键检查。
- 准备/白天松开移动 0.6 秒后自动站定；观察不打断，移动立即退出，E 仍可手动控制。手动解除额外留出 0.6 秒缓冲，夜晚不自动站定。
- 双方原已共享 breathOffset，未发现按观察角色隐藏呼吸的分支；振幅由 0.009 米调至 0.016 米。陪练可能在察觉威胁后主动屏息。近远视角的主观辨识度仍需试玩确认。
- 白昼由 420 秒缩至 300 秒，太阳轨迹不变；清除界面/结算中的旧时长常量。误射仍推进 35 秒，夜晚仍 45 秒。最终平衡需真人对局。
- 广播播放和生成配置均统一英文；旧中文 WAV 仅为历史资产，不再加载播放。界面继续支持中英文。


取消凶手 E 手动切换，保留自动站定和移动解除；移除原为手动解除设置的额外等待，中英文操作提示同步。警探 E 检查保留。


## 简短故事与误射试调

1934 年市政会计遇害、账页失踪的背景接入双方各自的准备开场，显示 8 秒并允许正文换行；四种结局延续警局涉案悬念。已有海报统一 1934 年，加入六件人像目录与匿名借展信息，不增加墙面装饰。误射推进从 35 秒改为 25 秒，四发合计 100 秒；更新当前说明及对应回归断言。未增加白昼近身反制；平衡为待真人试玩的试调。


## 房间记忆点

西画廊将两幅普通海报替换为五米宽的空画框，四条边分别参与碰撞，框心仍是原墙面。雕塑厅集中三尊既有人像并统一面朝北墙，全馆人像数量仍为六。东画廊增加高低两个封闭运输箱，共享移动、视线、弹道和镜头碰撞。沿用现有材质与照明；布局变化带来的局部遮挡属于本轮地图调整，未调整太阳参数。联机破坏测试改用共享雕像坐标，避免依赖旧位置。


## 2026-09-07 客户端预测

58 项测试通过。双客户端经 WebSocket 定时中转覆盖约 100/200/350ms RTT、各方向 ±10ms 有序抖动；验证确认前本地响应，停止后双方位置误差低于 0.12 米。另覆盖墙体、预测上限、阶段重置与权威状态隔离。未开展跨公网双机浏览器或丢包拥塞验收；尚无射击延迟补偿。


## 夜间选择与可读性

加入有视线/朝向/距离及气量条件的屏息提示；取消 Q 入口并拒绝联机 mark 操作。日光在最后 60 秒渐弱，闭馆 2.4 秒连续过渡，移除强制黑屏；日间共享暗部抬至 0.045。夜间自身可见材质仅切换凶手本机网格，保持深度遮挡，不改变对手材质。南门敞开并加入有阴影的照明。新增凶手逃离结局，保留警探夜间射击及双方撤离；45 秒支援到场沿用警探生存结算。增加夜间逃离及射击检查，主观明暗和出口引导仍待浏览器试玩验收。


## UI 可读性整理

主菜单底栏增加局部阅读底色，时间统一 17:55—18:00 与五分钟；开场改为准星上方的窄幅案卷卡，减小标题、增大正文、去掉文字光晕。资源/时间等区域使用局部底色，取消描边字；完整按键只在准备阶段显示，常态呼吸不重复提示，夜间凶手隐藏不可用的呼吸资源，近身才显示袭击提示。夜间时间条显示支援进度，最后十秒显示秒数。实际浏览器检查了主菜单和警探准备卡片可读性；夜间支援文案已出现，入口处调试入夜立即触发逃离结算，未声称完成完整夜间倒数视觉检查。构建通过。
# 2026-09-08 Dynamic audio validation

- Actual game launched at localhost:5100, with real browser user gestures. No AudioContext
  autoplay warnings or Console errors. Both imported MP3/OGG assets decoded.
- A normal unaccelerated browser game reached the existing ESCAPED ending when the
  detective remained by the south door. This was not changed into a support win.
- The separate two-context browser fixture completed PREPARATION at 15.00, DAY at
  315.00, SUNSET at 317.40 and SURVIVED at 362.45 game seconds. DAY alone used ×10
  test acceleration; actual configuration was never changed.
- Throughout SUNSET after its 100ms fade, both actual music gate gains stayed exactly
  zero. Final NIGHT layers drained by T-3. Each client triggered one support siren.
- Real breath test: 15s holding produced local heartbeat/tinnitus, exactly one server
  gasp, and cooldown. Detective breath danger stayed zero.
- Wrong shot advanced the clock by exactly 25s. Repeated events did not retrigger its
  480ms vacuum or mechanical cue. Invalid audio data retained fallback buffers.
- Observed pre-compressor sample peak including the shot: 0.0711; peak active one-shot
  count in the round: 4. This is sampled browser telemetry, not a mastering loudness scan.
- Two real WebSocket clients independently drove AudioDirector through all five phases
  and received matching final event IDs. Concealed-target identity/coordinates/breath
  changes produced identical detective music model output in automated tests.
- Final game UI checks: F2 released the pointer; DAY and Wrong Shot controls worked;
  SUNSET displayed 18:00 and closed the music gate. Production build passed.
- No human headphone audition was performed. Procedural breath/gasp, doors, clocks,
  relays and siren remain demo placeholders; existing hall IR remains ~0.95s.
